import styled from "@emotion/styled";
import { Menu } from "@mui/material";

export const StyledMenu = styled(Menu)({
	top: '10px',
	// right: '0',
	'& .MuiMenu-paper': {
    // right: '0',

    // width: 
	}
});
